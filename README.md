first download the x64 version of this https://github.com/linuxgamer/DLL-Injector.
Then on the drive you installed tf2 onto go to /run/media/yourname/probally will be home/SteamLibrary/steamapps/compatdata/440/pfx/drive_c/
next put the DLL_Injector-x64-Release.exe into the folder along with bypass.dll
lastly run this command in terminal bash -c "protontricks -c 'wine cmd /c \"cd /d C:\\\\ && DLL_Injector-x64-Release.exe bypass.dll tf_win64.exe\"' 440"
also ensuer you have proton tricks i use garuda so all the prerequisite stuff was already instead but if you dont have it you might need to install it
also run the command when the game is open
if you have any issues use this guide https://github.com/linuxgamer/Amalgam-Linux/wiki it is made for amalgam linux but it should be able to help with most problems you run into cause the injection 
steps are the same
WARNING: this is technically against the rules so you may have your account vac banned. dont blame me if that happened
